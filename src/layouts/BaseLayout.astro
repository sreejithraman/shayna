---
import { siteConfig, getConfigCSSProperties } from '@/config/site';
import { ClientRouter } from 'astro:transitions';
import Nav from '@/components/Nav.astro';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = `${siteConfig.name} — Percussionist`,
  description = siteConfig.description,
} = Astro.props;

const configStyles = getConfigCSSProperties();
const styleString = Object.entries(configStyles)
  .map(([key, value]) => `${key}: ${value}`)
  .join('; ');
---

<!doctype html>
<html lang="en" style={styleString}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <ClientRouter />
  </head>
  <body class="bg-void text-primary font-sans antialiased">
    <Nav />
    <main>
      <slot />
    </main>
    <script>
      import { initSmoothScroll } from '@/scripts/smooth-scroll';
      import { initHorizontalScroll } from '@/scripts/horizontal-scroll';
      import { initKenBurns } from '@/scripts/ken-burns';
      import { initTextReveal } from '@/scripts/text-reveal';
      import { initPageGrain } from '@/scripts/page-grain';
      import { initParallax } from '@/scripts/parallax';
      import { initDramaticReveal } from '@/scripts/dramatic-reveal';

      const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // Initialize smooth scroll
      if (!prefersReducedMotion) {
        initSmoothScroll();
      }

      // Initialize horizontal drag scroll
      initHorizontalScroll();

      // Initialize Ken Burns effect on photos
      if (!prefersReducedMotion) {
        initKenBurns();
      }

      // Initialize text reveal animation
      if (!prefersReducedMotion) {
        initTextReveal();
      }

      // Initialize full-page grain overlay
      initPageGrain();

      // Initialize parallax effects
      if (!prefersReducedMotion) {
        initParallax();
      }

      // Initialize dramatic reveal animations
      if (!prefersReducedMotion) {
        initDramaticReveal();
      }
    </script>
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
